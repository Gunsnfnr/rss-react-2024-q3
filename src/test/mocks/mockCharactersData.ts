import { CharactersData } from '../../components/Main/Main';
import { mockCharacter, mockCharacter2 } from './mockCharacters';

export const mockCharactersData: CharactersData = {
  results: [mockCharacter, mockCharacter2],
  previous: null,
  next: null,
};
